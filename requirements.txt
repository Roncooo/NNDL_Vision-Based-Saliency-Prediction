# pip mirror of environment.yml (spec §9). Conda is the primary path.
torch
torchvision
numpy
pandas
pillow
scipy
scikit-learn
matplotlib
tqdm
timm            # optional: transformer (Swin) backbone, Phase 3
